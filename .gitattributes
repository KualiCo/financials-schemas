# Auto detect text files and perform LF normalization. Will handle all files NOT found below
*          text=auto

# These files are text and should be normalized (Convert crlf => lf)
*.xml           text eol=lf
*.xsd           text eol=lf

#
# Common settings that generally should always be used with your language specific settings
#

# Documents
*.md       text eol=lf
*.txt      text eol=lf

# Scripts
*.bash     text eol=lf
*.sh       text eol=lf
# These are explicitly windows files and should use crlf
*.bat      text eol=crlf
*.cmd      text eol=crlf

# Serialisation
*.xml      text
*.yaml     text
*.yml      text

# Archives
*.7z       binary
*.gz       binary
*.tar      binary
*.tgz      binary
*.zip      binary

#
# Exclude files from exporting
#
.gitattributes export-ignore
.gitignore     export-ignore
.gitkeep       export-ignore
